import React from "react";
import { render } from "@testing-library/react";

import Hero from "./Hero";
var foo = "bar"
describe("<Hero />", () => {
  test("should display a hero section component", async () => {
    // ???
  });
});